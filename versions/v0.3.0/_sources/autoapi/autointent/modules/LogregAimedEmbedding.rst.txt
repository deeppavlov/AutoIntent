autointent.modules.LogregAimedEmbedding
=======================================

.. py:class:: autointent.modules.LogregAimedEmbedding(embedder_config = None, cv = 3, ft_config = None)

   Bases: :py:obj:`autointent.modules.base.BaseEmbedding`


   Module for configuring embeddings optimized for linear classification.

   The main purpose of this module is to be used at embedding node for optimizing
   embedding configuration using its logreg classification quality as a sort of proxy metric.

   :param embedder_config: Config of the embedder used for creating embeddings
   :param cv: Number of folds used in LogisticRegressionCV
   :param ft_config: settings for fine-tuning embeddings

   Examples:
   --------
   .. testcode::

       from autointent.modules.embedding import LogregAimedEmbedding
       utterances = ["bye", "how are you?", "good morning"]
       labels = [0, 1, 1]
       retrieval = LogregAimedEmbedding(
           embedder_config="sergeyzh/rubert-tiny-turbo",
           cv=2
       )
       retrieval.fit(utterances, labels)


   .. py:attribute:: name
      :value: 'logreg_embedding'


      Name of the module to reference in search space configuration.


   .. py:attribute:: supports_multiclass
      :value: True


      Whether the module supports multiclass classification


   .. py:attribute:: supports_multilabel
      :value: True


      Whether the module supports multilabel classification


   .. py:attribute:: supports_oos
      :value: False


      Whether the module supports oos data


   .. py:attribute:: cv
      :value: 3



   .. py:attribute:: ft_config


   .. py:method:: from_context(context, embedder_config = None, ft_config = None, cv = 3)
      :classmethod:


      Create a LogregAimedEmbedding instance using a Context object.

      :param context: Context containing configurations and utilities
      :param cv: Number of folds used in LogisticRegressionCV
      :param embedder_config: Config of the embedder to use
      :param ft_config: settings for fine-tuning embeddings



   .. py:method:: clear_cache()

      Clear embedder from memory.



   .. py:method:: fit(utterances, labels)

      Train the logistic regression model using the provided utterances and labels.

      :param utterances: List of text data to index
      :param labels: List of corresponding labels for the utterances



   .. py:method:: score_ho(context, metrics)

      Evaluate the embedding model using specified metric functions.

      :param context: Context containing test data and labels
      :param metrics: List of metric names to compute

      :returns: Dictionary of computed metric values for the test set



   .. py:method:: score_cv(context, metrics)

      Evaluate the embedding model using specified metric functions.

      :param context: Context containing test data and labels
      :param metrics: List of metric names to compute

      :returns: Dictionary of computed metric values for the test set



   .. py:method:: get_assets()

      Get the classifier artifacts for this module.

      :returns: EmbeddingArtifact object containing embedder information



   .. py:method:: predict(utterances)

      Predict probabilities for input utterances.

      :param utterances: List of texts to predict probabilities for

      :returns: Array of predicted probabilities


