autointent.modules.RetrievalAimedEmbedding
==========================================

.. py:class:: autointent.modules.RetrievalAimedEmbedding(embedder_config = None, k = 10)

   Bases: :py:obj:`autointent.modules.base.BaseEmbedding`


   Module for configuring embeddings optimized for retrieval tasks.

   The main purpose of this module is to be used at embedding node for optimizing
   embedding configuration using its retrieval quality as a sort of proxy metric.

   :param k: Number of nearest neighbors to retrieve
   :param embedder_config: Config of the embedder used for creating embeddings

   Examples:
   --------

   .. testcode::

       from autointent.modules.embedding import RetrievalAimedEmbedding
       utterances = ["bye", "how are you?", "good morning"]
       labels = [0, 1, 1]
       retrieval = RetrievalAimedEmbedding(
           k=2,
           embedder_config="sergeyzh/rubert-tiny-turbo",
       )
       retrieval.fit(utterances, labels)



   .. py:attribute:: name
      :value: 'retrieval'


      Name of the module to reference in search space configuration.


   .. py:attribute:: supports_multiclass
      :value: True


      Whether the module supports multiclass classification


   .. py:attribute:: supports_multilabel
      :value: True


      Whether the module supports multilabel classification


   .. py:attribute:: supports_oos
      :value: False


      Whether the module supports oos data


   .. py:attribute:: k
      :value: 10



   .. py:attribute:: embedder_config


   .. py:method:: from_context(context, embedder_config = None, k = 10)
      :classmethod:


      Create an instance using a Context object.

      :param context: The context containing configurations and utilities
      :param k: Number of nearest neighbors to retrieve
      :param embedder_config: Config of the embedder to use



   .. py:method:: fit(utterances, labels)

      Fit the vector index using the provided utterances and labels.

      :param utterances: List of text data to index
      :param labels: List of corresponding labels for the utterances



   .. py:method:: score_ho(context, metrics)

      Evaluate the embedding model using specified metric functions.

      :param context: Context containing test data and labels
      :param metrics: List of metric names to compute

      :returns: Dictionary of computed metric values for the test set



   .. py:method:: score_cv(context, metrics)

      Evaluate the embedding model using specified metric functions.

      :param context: Context containing test data and labels
      :param metrics: List of metric names to compute

      :returns: Dictionary of computed metric values for the test set



   .. py:method:: get_assets()

      Get the retriever artifacts for this module.

      :returns: A EmbeddingArtifact object containing embedder information



   .. py:method:: clear_cache()

      Clear cached data in memory used by the vector index.



   .. py:method:: predict(utterances)

      Predict the nearest neighbors for a list of utterances.

      :param utterances: List of utterances for which nearest neighbors are to be retrieved

      :returns: List of labels for each retrieved utterance


