autointent.modules.ArgmaxDecision
=================================

.. py:class:: autointent.modules.ArgmaxDecision

   Bases: :py:obj:`autointent.modules.base.BaseDecision`


   Argmax decision module.

   The ArgmaxDecision is a simple predictor that selects the class with the highest
   score (argmax) for single-label classification tasks.

   Examples:
   --------
   .. testcode::

       from autointent.modules import ArgmaxDecision
       import numpy as np
       predictor = ArgmaxDecision()
       train_scores = np.array([[0.2, 0.8], [0.7, 0.3]])
       labels = [1, 0]  # Single-label targets
       predictor.fit(train_scores, labels)
       test_scores = np.array([[0.1, 0.9], [0.6, 0.4]])
       decisions = predictor.predict(test_scores)
       print(decisions)

   .. testoutput::

       [1, 0]



   .. py:attribute:: name
      :value: 'argmax'


      Name of the module to reference in search space configuration.


   .. py:attribute:: supports_oos
      :value: False


      Whether the module supports oos data


   .. py:attribute:: supports_multilabel
      :value: False


      Whether the module supports multilabel classification


   .. py:attribute:: supports_multiclass
      :value: True


      Whether the module supports multiclass classification


   .. py:method:: from_context(context)
      :classmethod:


      Initialize from context.

      :param context: Context object containing configurations and utilities



   .. py:method:: fit(scores, labels, tags = None)

      Fit the predictor (no-op for ArgmaxDecision).

      :param scores: Array of shape (n_samples, n_classes) with predicted scores
      :param labels: List of true labels
      :param tags: List of Tag objects for mutually exclusive classes, or None

      :raises WrongClassificationError: If used on non-single-label data



   .. py:method:: predict(scores)

      Predict labels using argmax strategy.

      :param scores: Array of shape (n_samples, n_classes) with predicted scores

      :returns: List of predicted class indices

      :raises MismatchNumClassesError: If the number of classes does not match the trained predictor


