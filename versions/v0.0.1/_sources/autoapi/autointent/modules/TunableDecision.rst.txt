autointent.modules.TunableDecision
==================================

.. py:class:: autointent.modules.TunableDecision(target_metric = 'decision_accuracy', n_optuna_trials = 320, seed = 0, tags = None)

   Bases: :py:obj:`autointent.modules.base.BaseDecision`


   Tunable predictor module.

   TunableDecision uses an optimization process to find the best thresholds for predicting labels
   in single-label or multi-label classification tasks. It is designed for datasets with varying
   score distributions and supports out-of-scope (OOS) detection.

   :param target_metric: Metric to optimize during threshold tuning
   :param n_optuna_trials: Number of optimization trials
   :param seed: Random seed for reproducibility
   :param tags: Tags for predictions (if any)

   Examples:
   --------
   Single-label classification
   ===========================
   .. testcode::

       import numpy as np
       from autointent.modules import TunableDecision
       scores = np.array([[0.2, 0.8], [0.6, 0.4], [0.1, 0.9]])
       labels = [1, 0, 1]
       predictor = TunableDecision(n_optuna_trials=100, seed=42)
       predictor.fit(scores, labels)
       test_scores = np.array([[0.3, 0.7], [0.5, 0.5]])
       predictions = predictor.predict(test_scores)
       print(predictions)

   .. testoutput::

       [1, 0]

   Multi-label classification
   ==========================
   .. testcode::

       labels = [[1, 0], [0, 1], [1, 1]]
       predictor = TunableDecision(n_optuna_trials=100, seed=42)
       predictor.fit(scores, labels)
       test_scores = np.array([[0.3, 0.7], [0.6, 0.4]])
       predictions = predictor.predict(test_scores)
       print(predictions)

   .. testoutput::

       [[1, 0], [1, 0]]



   .. py:attribute:: name
      :value: 'tunable'



   .. py:attribute:: supports_multilabel
      :value: True



   .. py:attribute:: supports_multiclass
      :value: True



   .. py:attribute:: supports_oos
      :value: True



   .. py:attribute:: tags
      :type:  list[autointent.schemas.Tag] | None


   .. py:attribute:: target_metric
      :value: 'decision_accuracy'



   .. py:attribute:: n_optuna_trials
      :value: 320



   .. py:attribute:: seed
      :value: 0



   .. py:method:: from_context(context, target_metric = 'decision_accuracy', n_optuna_trials = 320)
      :classmethod:


      Initialize from context.

      :param context: Context containing configurations and utilities
      :param target_metric: Metric to optimize during threshold tuning
      :param n_optuna_trials: Number of optimization trials



   .. py:method:: fit(scores, labels, tags = None)

      Fit the predictor by optimizing thresholds.

      Note: When data doesn't contain out-of-scope utterances, using TunableDecision imposes
      unnecessary computational overhead.

      :param scores: Array of shape (n_samples, n_classes) with predicted scores
      :param labels: List of true labels
      :param tags: Tags for predictions (if any)



   .. py:method:: predict(scores)

      Predict labels using optimized thresholds.

      :param scores: Array of shape (n_samples, n_classes) with predicted scores

      :returns: Predicted labels (either single-label or multi-label)

      :raises MismatchNumClassesError: If number of classes in scores doesn't match training data


