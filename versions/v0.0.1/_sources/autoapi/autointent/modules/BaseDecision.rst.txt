autointent.modules.BaseDecision
===============================

.. py:class:: autointent.modules.BaseDecision

   Bases: :py:obj:`autointent.modules.base.BaseModule`, :py:obj:`abc.ABC`


   Base class for decision modules.


   .. py:method:: get_implicit_initialization_params()

      Return default params used in ``__init__`` method.

      Some parameters of the module may be inferred using context rather from ``__init__`` method.
      But they need to be logged for reproducibility during loading from disk.

      :returns: Dictionary of default params



   .. py:method:: fit(scores, labels, tags = None)
      :abstractmethod:


      Fit the model.

      :param scores: Scores to fit
      :param labels: Labels to fit
      :param tags: Tags to fit



   .. py:method:: predict(scores)
      :abstractmethod:


      Predict the best score.

      :param scores: Scores to predict

      :returns: Predicted labels



   .. py:method:: score_ho(context, metrics)

      Calculate metric on test set and return metric value.

      :param context: Context to score
      :param metrics: List of metrics to compute

      :returns: Dictionary of computed metrics values for the test set

      :raises RuntimeError: If no folded scores are found



   .. py:method:: score_cv(context, metrics)

      Calculate metric on test set and return metric value.

      :param context: Context to score
      :param metrics: List of metrics to compute

      :returns: Dictionary of computed metrics values for the test set

      :raises RuntimeError: If no folded scores are found



   .. py:method:: get_assets()

      Return useful assets that represent intermediate data into context.

      :returns: Decision artifact containing intermediate data



   .. py:method:: clear_cache()

      Clear cache.



   .. py:method:: get_train_data(context)

      Get training data from context.

      :param context: Context containing the data

      :returns: Tuple containing scores, labels, and tags


